# ArwaPark Frontend

Next.js (App Router) frontend scaffold for ArwaPark.

Quick start

1. cd frontend
2. npm install
3. copy `.env.example` to `.env.local` and set `NEXT_PUBLIC_API_URL`
4. npm run dev

API base URL default: `http://localhost:3000/api`
