# ArwaPark - Backend

Backend foundation for ArwaPark (NestJS + Prisma + PostgreSQL).

Quick start:

1. Set `DATABASE_URL` in `.env`
2. Install: `npm install`
3. Generate Prisma client: `npm run prisma:generate`
4. Run migrations & seed: `npm run prisma:migrate`
5. Start: `npm run start:dev`
